[
  {
    "fields": {
      "Name": "Sales: Deal Type Mix",
      "CategoryID": "@lookup:MJ: Query Categories.Name=Sales",
      "UserQuestion": "How is our business split between new, renewal and expansion motions?",
      "Description": "Mix by DealType (master plan §9.3 measure 10). Reads the type TABLE, not type names: rows group by DealTypeID and render the name for display only, so a deployment that adds a fourth motion or renames one gets a correct extra row rather than a miscounted one.",
      "SQL": "@file:SQL/deal-type-mix.sql",
      "TechnicalDescription": "Grain: Deal. RequiresRenewalSource is the one flag this branches on, and it is the same flag CloseDealOperation uses to route a renewal — so the mix and the routing agree by construction. RenewalsMissingSource surfaces renewal-type deals with no RenewsContractID, which means the routing had nothing to renew against. Dates: actual close for closed deals, expected for open, reported in separate columns so the two are never added.",
      "Status": "Approved",
      "QualityRank": 7,
      "ExecutionCostRank": 2,
      "UsesTemplate": true
    },
    "relatedEntities": {
      "MJ: Query Parameters": [
        {
          "fields": {
            "QueryID": "@parent:ID",
            "Name": "CompanyID",
            "Type": "string",
            "IsRequired": false,
            "Description": "Selling company. Omit for every company. Deal.CompanyID is server-stamped from the pipeline, so it is always present and never needs a fallback.",
            "SampleValue": "C0A5E100-0001-4A01-9E11-5B7C3D2F8A01",
            "DetectionMethod": "Manual"
          }
        },
        {
          "fields": {
            "QueryID": "@parent:ID",
            "Name": "PipelineID",
            "Type": "string",
            "IsRequired": false,
            "Description": "One pipeline. Omit for all pipelines.",
            "SampleValue": "90111111-0000-4000-A000-000000000001",
            "DetectionMethod": "Manual"
          }
        },
        {
          "fields": {
            "QueryID": "@parent:ID",
            "Name": "PeriodStart",
            "Type": "date",
            "IsRequired": false,
            "Description": "Inclusive start of the window. Omit for no lower bound.",
            "SampleValue": "2026-01-01",
            "DetectionMethod": "Manual"
          }
        },
        {
          "fields": {
            "QueryID": "@parent:ID",
            "Name": "PeriodEnd",
            "Type": "date",
            "IsRequired": false,
            "Description": "Inclusive end of the window. Omit for no upper bound.",
            "SampleValue": "2026-12-31",
            "DetectionMethod": "Manual"
          }
        }
      ]
    },
    "primaryKey": {
      "ID": "5A1E5000-0001-4000-A000-00000000000B"
    }
  }
]
